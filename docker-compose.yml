php:
  build: .
  ports:
    - "80:80"
  volumes:
    - .:/var/www/html/
  links:
    - mysql
mysql:
  environment:
    MYSQL_DATABASE: "fusion"
    MYSQL_ROOT_PASSWORD: "root"
  image: mysql:5.6
  ports:
    - "3306:3306"
